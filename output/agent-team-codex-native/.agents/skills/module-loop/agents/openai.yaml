interface:
  display_name: "Module Loop"
  short_description: "Advance one module through bounded evidence gates"
  default_prompt: "Use $module-loop to advance one bounded development module."
policy:
  allow_implicit_invocation: false
